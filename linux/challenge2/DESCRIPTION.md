The shell always has a current working directory.
Once you understand that idea, nested folders stop feeling like a maze and start feeling like coordinates.

This room is about navigating on purpose instead of guessing.
Move through the directory tree, confirm where you are, and treat `pwd` as a map instead of wandering blind.

**Your task:**
Navigate to `payphones/archive/central_office`, confirm that you are really there, read the note in that directory, and then verify the answer with:

```bash
./submit ANSWER
```
