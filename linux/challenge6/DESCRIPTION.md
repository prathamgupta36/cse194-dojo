Extensions lie in more than one way.
Sometimes the file is not just mislabeled, but actually a completely different container format with more files hidden inside it.

This room is about verifying the real file type first and only then choosing the right tool.
If you solve it cleanly, you are ready for the stranger filesystem tricks later in the pack.

**Your task:**
Determine what `dropbox.png` really is, unpack it correctly, recover the clue inside, and verify the answer with:

```bash
./submit ANSWER
```
