Real systems accumulate clutter fast.
Once a directory tree is large enough, manually opening folders one by one stops being a serious strategy.

This room is about recursive search.
Instead of browsing by hand, ask the filesystem to locate the exact file you care about and then read what it says.

**Your task:**
Locate the text file somewhere under this directory tree, recover the answer inside it, and verify it with:

*Hint* - Use the find command to locate any text files in the `/challenge` directory.

```bash
./submit ANSWER
```
